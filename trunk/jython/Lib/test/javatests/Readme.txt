The .java files were renamed to .jav files.  These files were in a directory two levels
below one pointed at by the classpath.  Eclipse did parse this classpath even though the 
subdirectory was in the classpath
